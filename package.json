{
  "name": "bun-docker-drizzle-elysia-litestream-sqlite",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build ./src/index.ts --compile --minify --target=bun --outfile=server"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.3",
    "elysia": "1.1.25"
  },
  "devDependencies": {
    "better-sqlite3": "^11.5.0",
    "bun-types": "latest",
    "drizzle-kit": "^0.28.1"
  },
  "module": "src/index.js"
}
